#include <iostream>
using namespace std;
int main()
{
    int  a, b, c;
    cout << "Input the number you wish to input:";
    cin >> c;

    for (a = 1; a <= c; a++)
    {
        for (b = 1; b <= a; b++)
        {
            cout << "* ";
        }

        cout << "\n";
    }
    return 0;
}
